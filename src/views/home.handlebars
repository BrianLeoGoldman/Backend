<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Home</title>
</head>
<body>
    <div class="home">
        <h1>Backend Coderhouse</h1>
        <h2>Checkout our products!</h2>
        <div class="products-list">
            {{#each products}}
            <div class="product-card">
                <p class="product-title">{{this.title}}</p>
                <p class="product-info">{{this.description}}</p>
                <p class="product-info">${{this.price}}</p>
                <p class="product-info">CODE: {{this.code}}</p>
            </div>
            {{/each}}
        </div>
    </div>
</body>
</html>